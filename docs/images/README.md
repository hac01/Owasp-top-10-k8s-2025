# README screenshots

Drop the two screenshots here so the main `README.md` renders them:

| Filename | What it shows |
|----------|---------------|
| `hero.png` | The landing page — "OWASP Kubernetes Top 10", 2025 edition hero. |
| `challenges.png` | The "10 challenges" grid (K01–K06 cards, difficulty + points). |

Use those exact filenames. PNG is expected; keep the width reasonable (~1600px)
so the repo stays small.
